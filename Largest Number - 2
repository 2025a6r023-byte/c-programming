#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
int A,B,C;
    scanf("%d%d%d",&A,&B,&C);
    if(A>=C&&B<=C){
    printf("%d",A);
}
    else if(B>=A&&C<=B){
    printf("%d",B);
}
    else{
    printf("%d",C);
}
    return 0;
}
