#include<stdio.h>
int main(){
    int num;
    printf("enter the value of num : ");
    scanf("%d",&num);
    while(num != 0){
        int digit = num%10;
        printf("%d\t",digit);
        num = num/10;
    }
    return 0;
}
