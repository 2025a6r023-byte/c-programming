#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b,c,d,e;
    float t,avg,p;
    
    scanf("%d%d%d%d%d",&a,&b,&c,&d,&e);
    t=a+b+c+d+e;
    avg=t/5;
    p=(t/500)*100;
    printf("%.0f %.2f %.2f",t,avg,p);

     
    return 0;
}
