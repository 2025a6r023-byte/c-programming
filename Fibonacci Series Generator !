#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int N,a=0,b=1,c,i;
    scanf("%d",&N);
    if(N==1)
        printf("%d",a);
    else
        printf("%d %d ",a,b);
    for(i=3;i<=N;i++){
        c=a+b;
        printf("%d ",c);
        a=b;
        b=c;
        }
    
    return 0;
}
