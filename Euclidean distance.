#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int x1,y1,x2,y2;
    float distance;
    scanf("%d%d%d%d",&x1,&y1,&x2,&y2);
    distance=pow((pow((x2-x1),2)+pow((y2-y1),2)),0.5);
    printf("%.2f",distance);
       
    return 0;
}
